import 'package:flutter/material.dart';

class Chicharrones extends StatelessWidget {
  const Chicharrones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
