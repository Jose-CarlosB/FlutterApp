import 'package:flutter/material.dart';

class SudadodePescado extends StatelessWidget {
  const SudadodePescado({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}